# docker-compose.yml
version: "3.9"
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      target: app
    expose: ["3000"]

  nginx:
    build:
      context: .
      dockerfile: Dockerfile
      target: nginx
    ports: ["6001:80"]
    depends_on: ["app"]
